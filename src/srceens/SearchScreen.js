import React from 'react';
import { Vew, Text, Button, StyleSheet } from 'react-native';

const SearchScreen = () => {
    return (
        <View>
            <Text>
                This is the Search Screen
    </Text>
        </View>
    );
};

const styles = StyleSheet.create({

});

export default SearchScreen;
